import React from "react";

import Modal from "../UI/Modal/Modal";
import styles from './Cart.module.css';

const Cart = (props) => {
  return (
    <Modal>
      <div>Hello there</div>
    </Modal>
  )
};

export default Cart;